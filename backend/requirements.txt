fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
sqlalchemy>=2.0
psycopg[binary]>=3.1
anthropic>=0.40   # search-term relevance LLM (used only when ANTHROPIC_API_KEY is set)
openpyxl>=3.1     # parse .xlsx budget-file uploads (CSV needs no extra deps)
google-cloud-bigquery>=3.20   # analytics warehouse (used only when GCP_PROJECT/BQ_DATASET are set)
sqlalchemy-bigquery>=1.11      # SQLAlchemy dialect for routing analytics reads to BigQuery
google-cloud-bigquery-storage>=2.24   # fast (Arrow) result downloads for BigQuery reads
pyarrow>=14                    # required by the BigQuery Storage read client
