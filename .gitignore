# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.env
.env.*

# Node / tooling (future)
node_modules/

# OS
.DS_Store
Thumbs.db

# Data — generated bundles are large and produced by the engine, not source.
# The legacy Mavis demo fixture was untracked in M0-A4; nothing reads data/clients at runtime.
data/clients/**/bundle.json

# Uploaded CSVs (user data, not source)
data/uploads/

# SQLite scratch
*.db
*.db-journal
